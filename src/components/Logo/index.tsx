import { Typography } from "@mui/material";

export function Logo() {
    return (
        <Typography
            variant="h5"
            sx={{
                fontWeight: "bold",
                color: "primary.main"
            }}>
            Lista de produtos
        </Typography>
    )
}
